2. 创建虚拟环境
```
py -3.11 -m venv venv
```

3. 升级pip
```
python -m pip install --upgrade pip
```

4. 激活虚拟环境
```
venv\Scripts\activate
```

5. 安装依赖
```
pip install -r ./requirements.txt
```